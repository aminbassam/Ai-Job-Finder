# ── Frontend (Vite) ────────────────────────────────────────────────────────────
# When VITE_API_URL is not set, the Vite dev server proxy handles /api → backend.
# Set this only for production builds where you want to point directly at the API.
# VITE_API_URL=https://api.yourdomain.com/api
